apiVersion: v1
kind: Service
metadata:
  name: omni-service
  namespace: homelab
spec:
  type: ExternalName
  externalName: ansible.centerionware.com
  ports:
  - name: http
    port: 443
    targetPort: 443
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: cloudflare-issuer
    kubernetes.io/ingress.class: traefik
    # traefik.ingress.kubernetes.io/router.middlewares: homelab-authentik@kubernetescrd # Optional
  name: omni-ingress
  namespace: homelab
spec:
  ingressClassName: traefik
  rules:
    - host: omni.centerionware.com
      http:
        paths:
          - backend:
              service:
                name: omni-service
                port:
                  number: 443
            path: / #Example.
            pathType: Prefix
  tls:
    - hosts:
        - centerionware.com
        - '*.centerionware.com'
      secretName: centerionware-default
